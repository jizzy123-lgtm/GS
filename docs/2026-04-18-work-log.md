# Work Log: 2026-04-17 to 2026-04-18

Coverage window:
- Committed work from `55eb9aa8`, `0b1ab2fe`, and `16fa05b6`
- Current working tree changes that are not yet committed as of `2026-04-18`

Recent commit checkpoints:
- `55eb9aa8` - `feat: update mobile app flows and notifications`
- `0b1ab2fe` - `feat: design sms pop up modal`
- `16fa05b6` - `fix: restore requester maintenance form ui`

Current working tree:
- `app/screens/AssignScheduleScreen.jsx`
- `app/screens/DashboardScreen.jsx`
- `app/screens/ReviewRequestsScreen.jsx`
- `app/screens/SubmitRequestScreen.jsx`
- `app/screens/ViewRequestStatusScreen.jsx`
- `utils/maintenanceRequests.js`

## File-by-File Notes

### App setup and routing

- `app.json`
  - Added Expo plugin configuration used by the recent maintenance-request and login-tracking work.
  - Added image-picker permission text for maintenance attachments.
  - Added location permission text for login-location tracking support.
  - Includes Android `googleServicesFile` configuration used by the notification flow.

- `package.json`
  - Added runtime dependencies used by the recent features, especially `expo-location` and `expo-notifications`.
  - Kept `expo-image-picker` in active use for maintenance request attachments.

- `package-lock.json`
  - Lockfile updated to match the dependency changes above.

- `app/_layout.tsx`
  - Added notification handler setup so push notifications can be displayed while the app is running.
  - Added notification-tap routing so a tapped notification can open the app and forward request-related params into the app shell.

- `app/(tabs)/index.tsx`
  - Expanded the local screen registry to include `Notifications`, `Feedbacks`, `LoginLocationTracking`, `ReviewRequests`, `AssignSchedule`, and related screens.
  - Wired notification-driven request navigation using `notifRequestId` and `notifRoleId`.
  - Passed `user` into screens that now need role-aware behavior and request-id-aware navigation.

- `app/screens/index.tsx`
  - Mirrored the same local screen routing updates used in `app/(tabs)/index.tsx`.
  - Added navigation support for notifications, feedbacks, login tracking, review requests, and schedule assignment.

### Shared utilities and reusable UI

- `app/components/AttachedImagesSection.jsx`
  - Added a reusable image-preview block for maintenance request detail screens.
  - Supports horizontal thumbnails, modal preview, and opening the original file in the browser.

- `app/components/SmsPopup.jsx`
  - Added a reusable SMS compose modal with message input, cancel/send actions, and a polished popup layout.
  - Used as the new SMS popup design work from the recent session.

- `app/constants/maintenanceStatus.js`
  - Centralized maintenance status constants and status-id mappings.
  - Added normalization logic so backend values like `approved`, `scheduled`, `completed`, `denied`, and `cancelled` resolve consistently across screens.

- `utils/imageAttachments.js`
  - Centralized maintenance attachment rules and FormData helpers.
  - Supports up to 4 image files.
  - Validates `jpeg`, `jpg`, `png`, and `gif`.
  - Raised max file size to `5MB` per image.
  - Added URL normalization helpers used by detail screens and image previews.

- `utils/loginLocation.js`
  - Added a non-blocking login-location helper for `POST /api/login`.
  - Reads foreground permission status without forcing login to wait on permission approval.
  - Sends `latitude`, `longitude`, and best-effort `address` when available.
  - Times out safely so login still succeeds if location is slow or unavailable.

- `utils/notificationNavigation.js`
  - Added notification payload parsing for `request_id`, `maintenance_request_id`, and similar variants.
  - Resolves the proper in-app navigation target based on the authenticated user's role.
  - Prevents routing mistakes when notification payload role and current device user role do not match.

- `utils/maintenanceRequests.js`
  - Added shared helpers for auth headers, token lookup, list/item extraction, maintenance type labeling, API error parsing, and requester-role inspection.
  - Added merge logic used when combining list payloads with role-detail payloads.
  - Current uncommitted work:
    - Added `getRequestId(...)` to normalize request identifiers, preferring `request_id` from backend payloads.
    - Updated merge behavior to preserve normalized `id` and `request_id` after detail payload merges.

### Auth and login-location tracking

- `app/LoginScreen.jsx`
  - Updated login to append optional location data from `utils/loginLocation.js`.
  - Login still succeeds even if location is unavailable.
  - Stores normalized `role_id` locally after auth.
  - Registers push notifications with the backend after successful login.

- `app/screens/LoginLocationTrackingScreen.jsx`
  - Added an admin-only screen for login-location tracking settings and audit logs.
  - Loads `GET /api/settings`.
  - Toggles `POST /api/settings/toggle-location-tracking`.
  - Reads `GET /api/login-locations`.
  - Handles unauthorized and validation-style errors through the shared maintenance request helpers.

### Notifications and request deep-linking

- `app/screens/NotificationsScreen.jsx`
  - Added notification list UI with unread state.
  - Added mark-one-read and mark-all-read behavior.
  - Added request-aware tap behavior so notification taps navigate into the correct request screen when possible.
  - Added fallback alert behavior when a notification does not contain a navigable request payload.

### Dashboard and overview flows

- `app/screens/DashboardScreen.jsx`
  - Expanded role-based quick actions.
  - Added `New Request` and `My Requests` quick actions for Head and Staff.
  - Added `Login Tracking` quick action for Admin.
  - Added routing into request-status views from dashboard KPIs and recent request rows.
  - Current uncommitted work:
    - Recent request items are normalized so `request_id` payloads still navigate correctly into request details.

### Maintenance request submission

- `app/screens/SubmitRequestScreen.jsx`
  - Reworked maintenance request submission to use the live backend contracts:
    - `GET /api/users/reqInfo`
    - `GET /api/maintenance-types`
    - `POST /api/maintenance-requests`
  - Uses account-linked `user_id`, `position_id`, `office_id`, and `contact_number` in the payload.
  - Uses JSON when there are no images and multipart when there are images.
  - Added role-aware Staff/Head internal submission experience during the UI redesign phase.
  - Added image evidence upload support, helper text, and role-aware workflow notes.
  - Commit `16fa05b6` restored the old requester UI exactly.
  - Current uncommitted work:
    - Staff has now also been restored to the old legacy request form UI.
    - Head remains on the newer internal-office request form.
    - The screen now splits between:
      - legacy path for Requester and Staff
      - internal path for Head

### Request list and detail flows

- `app/screens/ViewRequestStatusScreen.jsx`
  - Added request listing and in-screen request detail view for requester and non-requester roles.
  - Added support for image preview via `AttachedImagesSection`.
  - Added cancel-request behavior for requester-owned pending requests.
  - Added feedback routing for done requests.
  - Current uncommitted work:
    - Normalizes request payloads so backend responses that use `request_id` instead of `id` still render and navigate correctly.
    - Detail fetches now use normalized request ids, preventing `/api/staffpov/undefined`.
    - Cancel and feedback flows now use normalized request ids as well.

- `app/screens/ReviewRequestsScreen.jsx`
  - Updated the review flow to prefer `GET /api/maintenance-requests/list-with-details` with fallback to the base request list endpoint.
  - Added detail merging from role-specific endpoints:
    - `GET /api/staffpov/{id}`
    - `GET /api/headpov/{id}`
    - `GET /api/directorpov/{id}`
  - Added role-aware sequential review behavior for Staff, Head, and Campus Director.
  - Implemented the special Head-submitted workflow:
    - Staff verifies
    - skip Head approval
    - Campus Director approves
  - Added priority assignment and schedule handoff controls.
  - Current uncommitted work:
    - Normalizes list items so `request_id` becomes the canonical request id in the screen state.
    - All review actions now use normalized request ids.
    - Assign-schedule navigation now passes the normalized request id.
    - Detail merge logic now preserves the normalized request id after role-detail fetches.

- `app/screens/AssignScheduleScreen.jsx`
  - Updated schedule assignment logic to work with the newer approval pipeline.
  - Scheduling readiness now respects director-approved pending requests that already have a priority assigned.
  - Added attachment preview support for the selected request.
  - Added fallback role-detail image fetch support.
  - Current uncommitted work:
    - Normalizes request ids from request-list payloads that use `request_id`.
    - Schedule assignment now posts to the backend using the normalized request id.
    - Selection state and detail image loading now use normalized request ids.

### Feedback flows

- `app/screens/FeedbackScreen.jsx`
  - Added requester feedback submission flow for completed requests.
  - Filters request choices to scheduled/done requests owned by the current user.
  - Prevents duplicate feedback submission for requests that already have feedback.

- `app/screens/FeedbacksScreen.jsx`
  - Added feedback list/detail view for completed and scheduled requests.
  - Extracts request ids and feedback ids from multiple possible backend payload shapes.
  - Supports embedded feedback, separate feedback lists, and fallback detail resolution.

### Other documentation

- `docs/2026-04-11.md`
  - Existing project notes file updated during the mobile flow / notification work window.

## Current Uncommitted Delta Summary

These are the changes still present in the working tree after the last push:

- `utils/maintenanceRequests.js`
  - New request-id normalization helper.
  - Merge logic now preserves normalized request ids.

- `app/screens/ViewRequestStatusScreen.jsx`
  - Fixed list/detail bug caused by backend payloads using `request_id` instead of `id`.
  - Prevents request details from showing `#undefined` and calling `/api/staffpov/undefined`.

- `app/screens/ReviewRequestsScreen.jsx`
  - Normalized request ids across list rows, detail fetches, approval actions, and schedule navigation.

- `app/screens/AssignScheduleScreen.jsx`
  - Normalized request ids for request selection and schedule submission.

- `app/screens/DashboardScreen.jsx`
  - Recent request card navigation now uses normalized request ids.

- `app/screens/SubmitRequestScreen.jsx`
  - Staff UI restored to the old simpler form.
  - Requester remains on the old simpler form.
  - Head still uses the newer internal form.

## Verification Notes

- Recent verification command used during this work window:
  - `npm.cmd run lint`
- Latest lint state:
  - no errors
  - one existing warning remains in `app/screens/DashboardScreen.jsx` for the `fetchDashboard` hook dependency
